# T606 Screenshot Note (2026-03-06)

## Scope

- list empty-state copy
- mobile header density
- settings operator boundary emphasis

## Evidence

- `npm --prefix webapp run lint`
- `npm --prefix webapp run i18n:check`
- `npm --prefix webapp run build`

## Visual checkpoints

- header no longer exposes the install CTA directly in the app bar
- compact mobile header keeps `menu + brand + drive + language`
- empty-state copy describes the two available actions without location-based instructions
- operator boundary card reads as internal-only through outline/helper text, not through heavy decoration

## Notes

- No new screenshots were captured in this turn.
- These checkpoints should be re-validated visually after deployment on mobile and desktop browsers.
